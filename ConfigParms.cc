#include <iostream>
#include "ConfigParms.h"

using namespace std;


//called by the driver to read and parse the configuration file

void ConfigParms::processConfigFile( const char *configFile, ConfigParms &cparms ) {

}